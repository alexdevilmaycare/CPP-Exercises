#include<iostream>
#include<string>

int main () {
    std::getline (std::cin, const std::string & str,)
}


/* #include<deque>
#include<iostream>

void Print ( const std::deque <int> & collection ) {
    for ( const auto & item: collection ) {
        std::cout << item << " "; 
    }
    std::cout << "\n";
}

int main () {
    std::deque <int> queuedCollection = {1, 2, 3, 4, 5};
    Print(queuedCollection); 
    queuedCollection.resize(3);
    Print(queuedCollection); 
    return 0; 
} */